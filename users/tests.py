from django.test import TestCase

# Create your tests here.

# a = [20, 4100, 8300]
# b = {'0': 2, '1': 1, '2': 6, '3': 0, '4': 4, '5': 3, '9': 5, '11': 7}
#
# chunk_size = 4096
#
# for i in a:
#     page_num = i // chunk_size
#     page_offset = i % chunk_size
#     abs_add = b[str(page_num)] * chunk_size + page_offset
#     print(str(i)+'的页号为:'+str(page_num)+'\t'+'页内偏移为:'+str(page_offset)+'\t'+'绝对地址为:'+str(abs_add)+'\n')

